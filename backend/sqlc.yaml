
version: "2"
sql:
  - engine: "postgresql"
    queries: "db/queries"
    schema: "db/migrations"
    gen:
      go:
        sql_package: "pgx/v4"
        package: "db"
        out: "db"
        rename:
          isrc: ISRC